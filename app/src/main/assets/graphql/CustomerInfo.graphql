query customerInfo {
    customerInfo {
        authInfo {
            status,
            message
        },
        customerType,
        subscriptionType,
        cancellation,
        sampleType
    }
}
