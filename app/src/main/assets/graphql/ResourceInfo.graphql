query ResourceInfoQuery {
    product {
        authInfo {
            status
            message
        }
        resourceVersion
        resourceBaseUrl
        resourceList {
            name
            storageType
            moTime
            sha256
            size
        }
        resourceZip
    }
}
