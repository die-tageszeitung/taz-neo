query FeedQuery($name: String) {
    product {
        feedList(name: $name) {
            name
            cycle
            momentRatio
            issueCnt
            issueMaxDate
            issueMinDate
        }
    }
}