mutation Cancellation(
    $isForce: Boolean
) {
    cancellation(
        isForce: $isForce,
    ) {
        tazIdMail
        cancellationLink
        info {
            aboId
            tazId
            noAuthToken
            elapsed
            specialAccess
        }
    }
}