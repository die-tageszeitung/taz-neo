query ResourceInfoQuery {
    product {
        resourceVersion
        resourceBaseUrl
        resourceList {
            name
            storageType
            moTime
            sha256
            size
        }
        resourceZip
    }
}
