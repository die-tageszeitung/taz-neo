mutation TrialSubscription(
    $tazId: String!,
    $idPassword: String!,
    $surname: String,
    $firstName: String,
    $installationId: String!,
    $deviceType: DeviceType,
    $pushToken: String
) {
    trialSubscription(
        tazId: $tazId,
        idPw: $idPassword,
        surname: $surname,
        firstName: $firstName,
        installationId: $installationId,
        deviceType: $deviceType
        pushToken: $pushToken
    ) {
        status
        message
        token
    }
}