mutation TrialSubscription(
    $tazId: String!,
    $idPassword: String!,
    $surname: String,
    $firstName: String,
    $nameAffix: String,
    $installationId: String!,
    $deviceType: DeviceType,
    $pushToken: String
) {
    trialSubscription(
        tazId: $tazId,
        idPw: $idPassword,
        surname: $surname,
        firstName: $firstName,
        installationId: $installationId,
        deviceType: $deviceType
        pushToken: $pushToken,
        nameAffix: $nameAffix
    ) {
        status
        message
        token
    }
}