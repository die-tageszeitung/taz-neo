mutation SubscriptionReset(
    $subscriptionId: Int!,
    $appVersion: String!,
    $deviceFormat: DeviceFormat!,
    $deviceName: String,
    $deviceType: DeviceType!,
    $deviceVersion: String
) {
    subscriptionReset(
        subscriptionId: $subscriptionId,
        appVersion: $appVersion,
        deviceFormat: $deviceFormat,
        deviceName: $deviceName,
        deviceType: $deviceType,
        deviceVersion: $deviceVersion
    ) {
        status
        mail
    }
}